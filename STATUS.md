# STATUS.md — ChairMatch Repo & Deploy

_Generated by `scripts/status.sh` at **2026-08-28 18:36:22 CEST**_

Dies ist die zentrale Wahrheits-Quelle für den aktuellen Stand.
**Nicht manuell editieren** — der Inhalt wird bei jedem `./deploy.sh` neu geschrieben.

---

## Git

| Feld | Wert |
|------|------|
| Branch | `main` |
| HEAD | `2fdfe6d` (`2fdfe6d40149a3d73439fe0abce97da8864da34c`) |
| Letzter Commit | Track 23: Benachrichtigungswege — Push, Postfach, Warteliste, Einwilligung. Sieben Befunde, alle behoben, einer zusaetzlich als committete Migration. Drei davon haben woertlich dieselbe Ursache: 42P10, 'there is no unique or exclusion constraint matching the ON CONFLICT specification'. Postgres verlangt fuer INSERT ... ON CONFLICT (a,b) einen UNIQUE-Index auf GENAU diesen Spalten; findet es keinen, ist das kein stiller Rueckfall auf ein gewoehnliches INSERT, sondern ein harter Fehler. (1) P1: Die Warteliste ist ein schwarzes Loch mit Erfolgsmeldung. POST /api/wait-list ist oeffentlich und die einzige Stelle, an der Interessenten in Staedten ohne Angebot eingesammelt werden. Sie schrieb upsert(onConflict:'email,city'), der einzige UNIQUE-Index der Tabelle ist aber ein AUSDRUCKS-Index auf (email, COALESCE(city,'')) — kein Kandidat. Jeder Eintrag lief in 42P10. Der Rueckfall darunter hat es nicht aufgefangen, sondern verdeckt: er schrieb nach 'newsletter', das live eine VIEW ist (Sonde: 'permission denied for VIEW newsletter'), auf der ein ON CONFLICT gar nicht moeglich ist — und er stand in einem try/catch, das supabase-js nie ausloest, weil es bei DB-Fehlern nicht wirft, sondern {error} zurueckgibt. Danach lief die Funktion weiter bis 'return ok:true'. Seit dem 15.05.2026 hat jeder Eingetragene eine Bestaetigung gesehen und steht nirgends; notified_at ist eine Spalte, die nie jemand fuellen konnte. Jetzt nachsehen-dann-schreiben ohne ON CONFLICT, 23505 als Erfolg (Endzustand stimmt), jeder andere Fehler als 503, toter newsletter-Rueckfall entfernt, Stadt auf eine Schreibweise normalisiert, Zaehler des IP-Limits winkt bei Ausfall nicht mehr durch. (2) P2: Die Push-Anmeldung schlaegt zu 100 Prozent fehl, aus zwei unabhaengigen Gruenden — push_subscriptions.updated_at gibt es live NICHT (Sonde 28.08.: ?select=updated_at -> 42703, waehrend created_at 42501 liefert), und der einzige UNIQUE-Index steht auf 'endpoint' ALLEIN, ON CONFLICT (user_id,endpoint) findet dafuer keinen Arbiter. Die Funktion warf, die Route antwortete 500, die Tabelle konnte nie eine Zeile bekommen — also lieferte sendPushNotification ausnahmslos sent:0 und /api/push/send meldete dem Admin 'success:true'. Ein upsert waere auch nach der Reparatur falsch gewesen: einen Endpunkt, der einem ANDEREN Konto gehoert, haette er stillschweigend umgehaengt. Jetzt Lookup, kein updated_at, fremder Endpunkt 409, Deckel 20 Geraete je Konto, 10/h Limit. (3) P2: SSRF — der Push-Endpunkt war eine frei waehlbare URL, die der Server abruft. subscribe nahm jede Zeichenkette bis 2000 Zeichen, sendPushNotification macht daraus fetch(sub.endpoint). Ein angemeldetes Konto bestimmte damit das Ziel einer Anfrage aus unserem Rechenzentrum: 169.254.169.254 (Hoster-Metadaten), 127.0.0.1:3000 (unsere eigenen Routen von innen), jede interne Adresse — blind, aber mit unserem VAPID-Header. Erreichbar war das bisher NUR, weil (2) die Tabelle leer hielt: wer (2) allein repariert, schaltet (3) scharf. Neuer Baustein src/lib/push-endpoint.ts mit Positivliste (FCM, Mozilla, Apple, WNS), https erzwungen, keine Anmeldedaten, kein abweichender Port, keine IP-Literale — geprueft an beiden Enden, beim Speichern UND vor dem fetch. (4) P2: Push-Nutzdaten gingen unverschluesselt hinaus. Der Koerper war Klartext-JSON mit 'Content-Encoding: aes128gcm' im Kopf; der Zweck der Verschluesselung in Web Push ist nicht die Leitung, sondern der ZUSTELLDIENST — Google, Mozilla und Apple leiten weiter und sollen den Inhalt nicht lesen, und der Inhalt sind Termine, Betraege und Bestellnummern. Das VAPID-Token war ausserdem unbrauchbar: createSign('SHA256') liefert eine DER-Signatur, JWS/ES256 verlangt rohes r||s mit 64 Byte — und soweit waere es nie gekommen, weil der zusammengesetzte PEM-Block aus DER-Kopf 30770201010420 plus 32-Byte-Schluessel bestand und dann aufhoerte (0x77=119 Byte kuendigen zusaetzlich Kurven-OID und oeffentlichen Punkt an: 3+34+12+70). Beide sign()-Versuche warfen, der Aufrufer zaehlte failed++. Einen push-Handler gibt es ausserdem nirgends: public/sw.js ist ein Selbstzerstoerer, keine Client-Datei ruft pushManager.subscribe(). Neuer Baustein src/lib/web-push.ts mit RFC 8291 (ECDH P-256, HKDF, AES-128-GCM) und RFC 8292 (ieee-p1363, SEC1-DER vollstaendig). Der Code sendet lieber nichts als etwas Unverschluesseltes: Konfigurationsfehler bricht ab, Route antwortet 503. Nachweis mit dem Schluesselmaterial aus RFC 8291 Abschnitt 5 — der erzeugte Kopf stimmt Byte fuer Byte mit dem Beispiel der Norm, UND der private Schluessel der Norm macht unseren Chiffretext auf und liefert ihren Klartext. (5) P2: Die DSGVO-Loeschung erreicht die Zustellwege nicht. notification_log und push_subscriptions haengen per ON DELETE CASCADE an profiles — nur wird profiles in keinem Loeschpfad geloescht, sondern anonymisiert, die Kaskade feuert also nie. Nach einer vollstaendigen Loeschung nach Art. 17 standen weiter da: das Postfach mit Datum, Betrag und Bestellnummer, der Geraete-Endpunkt als aktives Zustellziel, und die E-Mail-Adresse in wait_list, die von keinem Loeschpfad je angefasst wurde (die Warteliste kennt kein Konto und hat nirgends eine Abmeldung). Jetzt: account/delete loescht die Push-Abos sofort und traegt die Adresse aus der Warteliste aus, solange sie bekannt ist (dasselbe Update leert profiles.email); der hard-delete-Cron raeumt notification_log und push_subscriptions. (6) P2, per Musterabgleich gefunden: nach dem ersten 42P10-Befund wurde jede der neun upsert-Stellen gegen ihren Index geprueft. Sechs sind in Ordnung. POST /api/favorites war es nicht: der Index aus 20260827_favorites_equipment.sql ist PARTIELL (WHERE equipment_id IS NOT NULL), und ein partieller Index kommt als Arbiter nur mit mitgeliefertem Praedikat in Frage — PostgREST schickt ueber on_conflict= nur die Spaltenliste, also nie. favorites.equipment_id ist live vorhanden, die Migration mithin eingespielt: jeder Klick auf 'Inserat merken' lief in 42P10 und kam als 500 zurueck. Die Salon-Seite derselben Route war nicht betroffen (voller Constraint). Jetzt reines INSERT — braucht keinen Arbiter, und 'schon gemerkt' galt dort seit jeher als Erfolg. (7) P3: /api/cookies/consent gab die rohe DB-Meldung zurueck (letzter Nachzuegler aus Track 18/19, auf einer unangemeldeten Route), hatte kein eigenes Limit, und liess ip_hash leer — eine Spalte, die live genau dafuer existiert. Ein Einwilligungsnachweis ohne Zuordnung ist keiner; die session_id kommt aus dem Browser. Jetzt dbError(), 20/h je IP, HMAC statt IP. Nachtrag: die Kachel 'Benachrichtigungen' im Konto trug fest verdrahtet 'Aktiv' — aus keiner Quelle gelesen, und einen Schalter dazu gibt es nirgends. Ohne Befund: /api/notifications (beide Zweige filtern auf user_id aus der Session), Empfaengerwahl aller acht createNotification-Aufrufe samt CHECK-Positivliste der Typen, anon-Rechte auf allen vier Tabellen (42501), Admin-Pruefung von push/send, analytics/visit. Nicht abschliessend pruefbar: customer_salon_history hat im Repo kein CREATE TABLE. WERKZEUG — der eigentliche Grund, warum nichts davon aufgefallen ist: src/test/fake-supabase.ts kannte gar kein upsert() (wer einen Test schreiben wollte, bekam 'upsert is not a function' und liess es), und die zweite Fassung im e2e-Harness nahm die Konfliktspalten einfach als Schluessel, ohne nach einem Index zu fragen. Beide nachgezogen: 42P10 statt stillem Rueckfall, Ausdrucks-Indizes bewusst gar nicht darstellbar, dazu addUniqueIndex()/requireArbiterIndex() (opt-in) und echte 23505-Durchsetzung beim INSERT inklusive NULL-Semantik und partieller Indizes. schema-probe.sh und live-schema.ts um push_subscriptions, wait_list und cookie_consents erweitert, favorites.equipment_id korrigiert. Zwei Bestandstests angepasst (Track-18-Mock von saveSubscription lieferte void, die Route liest jetzt ein Ergebnis; ip-hash-Zeitlimit auf 30s wegen Flake im vollen Lauf). Gegenprobe: mit auf bf6fcf6 zurueckgesetztem Produktivcode fallen 21 der 48 neuen Tests durch. 48 neue Tests (1526 -> 1574), Bericht und PDF in docs/reports/. ANMERKUNG: eine parallele Session im selben Arbeitsverzeichnis hat mit git add -A neun Dateien dieses Tracks in Commit 5af4013 eingesammelt, der von etwas anderem handelt — inhaltlich ist nichts verloren. Aus derselben Quelle: die CM22-Migration ist live angewendet (nachgeprueft: rental_equipment antwortet anon jetzt 401). |
| Autor | Yusuf <y.cilcioglu@googlemail.com> |
| Datum | 2026-08-28 18:35:59 +0200 |
| Uncommitted Changes | 0 |
| Ahead of origin/main | 0 commits |
| Behind origin/main | 0 commits |

### Letzte 5 Commits

```
2fdfe6d Track 23: Benachrichtigungswege — Push, Postfach, Warteliste, Einwilligung. Sieben Befunde, alle behoben, einer zusaetzlich als committete Migration. Drei davon haben woertlich dieselbe Ursache: 42P10, 'there is no unique or exclusion constraint matching the ON CONFLICT specification'. Postgres verlangt fuer INSERT ... ON CONFLICT (a,b) einen UNIQUE-Index auf GENAU diesen Spalten; findet es keinen, ist das kein stiller Rueckfall auf ein gewoehnliches INSERT, sondern ein harter Fehler. (1) P1: Die Warteliste ist ein schwarzes Loch mit Erfolgsmeldung. POST /api/wait-list ist oeffentlich und die einzige Stelle, an der Interessenten in Staedten ohne Angebot eingesammelt werden. Sie schrieb upsert(onConflict:'email,city'), der einzige UNIQUE-Index der Tabelle ist aber ein AUSDRUCKS-Index auf (email, COALESCE(city,'')) — kein Kandidat. Jeder Eintrag lief in 42P10. Der Rueckfall darunter hat es nicht aufgefangen, sondern verdeckt: er schrieb nach 'newsletter', das live eine VIEW ist (Sonde: 'permission denied for VIEW newsletter'), auf der ein ON CONFLICT gar nicht moeglich ist — und er stand in einem try/catch, das supabase-js nie ausloest, weil es bei DB-Fehlern nicht wirft, sondern {error} zurueckgibt. Danach lief die Funktion weiter bis 'return ok:true'. Seit dem 15.05.2026 hat jeder Eingetragene eine Bestaetigung gesehen und steht nirgends; notified_at ist eine Spalte, die nie jemand fuellen konnte. Jetzt nachsehen-dann-schreiben ohne ON CONFLICT, 23505 als Erfolg (Endzustand stimmt), jeder andere Fehler als 503, toter newsletter-Rueckfall entfernt, Stadt auf eine Schreibweise normalisiert, Zaehler des IP-Limits winkt bei Ausfall nicht mehr durch. (2) P2: Die Push-Anmeldung schlaegt zu 100 Prozent fehl, aus zwei unabhaengigen Gruenden — push_subscriptions.updated_at gibt es live NICHT (Sonde 28.08.: ?select=updated_at -> 42703, waehrend created_at 42501 liefert), und der einzige UNIQUE-Index steht auf 'endpoint' ALLEIN, ON CONFLICT (user_id,endpoint) findet dafuer keinen Arbiter. Die Funktion warf, die Route antwortete 500, die Tabelle konnte nie eine Zeile bekommen — also lieferte sendPushNotification ausnahmslos sent:0 und /api/push/send meldete dem Admin 'success:true'. Ein upsert waere auch nach der Reparatur falsch gewesen: einen Endpunkt, der einem ANDEREN Konto gehoert, haette er stillschweigend umgehaengt. Jetzt Lookup, kein updated_at, fremder Endpunkt 409, Deckel 20 Geraete je Konto, 10/h Limit. (3) P2: SSRF — der Push-Endpunkt war eine frei waehlbare URL, die der Server abruft. subscribe nahm jede Zeichenkette bis 2000 Zeichen, sendPushNotification macht daraus fetch(sub.endpoint). Ein angemeldetes Konto bestimmte damit das Ziel einer Anfrage aus unserem Rechenzentrum: 169.254.169.254 (Hoster-Metadaten), 127.0.0.1:3000 (unsere eigenen Routen von innen), jede interne Adresse — blind, aber mit unserem VAPID-Header. Erreichbar war das bisher NUR, weil (2) die Tabelle leer hielt: wer (2) allein repariert, schaltet (3) scharf. Neuer Baustein src/lib/push-endpoint.ts mit Positivliste (FCM, Mozilla, Apple, WNS), https erzwungen, keine Anmeldedaten, kein abweichender Port, keine IP-Literale — geprueft an beiden Enden, beim Speichern UND vor dem fetch. (4) P2: Push-Nutzdaten gingen unverschluesselt hinaus. Der Koerper war Klartext-JSON mit 'Content-Encoding: aes128gcm' im Kopf; der Zweck der Verschluesselung in Web Push ist nicht die Leitung, sondern der ZUSTELLDIENST — Google, Mozilla und Apple leiten weiter und sollen den Inhalt nicht lesen, und der Inhalt sind Termine, Betraege und Bestellnummern. Das VAPID-Token war ausserdem unbrauchbar: createSign('SHA256') liefert eine DER-Signatur, JWS/ES256 verlangt rohes r||s mit 64 Byte — und soweit waere es nie gekommen, weil der zusammengesetzte PEM-Block aus DER-Kopf 30770201010420 plus 32-Byte-Schluessel bestand und dann aufhoerte (0x77=119 Byte kuendigen zusaetzlich Kurven-OID und oeffentlichen Punkt an: 3+34+12+70). Beide sign()-Versuche warfen, der Aufrufer zaehlte failed++. Einen push-Handler gibt es ausserdem nirgends: public/sw.js ist ein Selbstzerstoerer, keine Client-Datei ruft pushManager.subscribe(). Neuer Baustein src/lib/web-push.ts mit RFC 8291 (ECDH P-256, HKDF, AES-128-GCM) und RFC 8292 (ieee-p1363, SEC1-DER vollstaendig). Der Code sendet lieber nichts als etwas Unverschluesseltes: Konfigurationsfehler bricht ab, Route antwortet 503. Nachweis mit dem Schluesselmaterial aus RFC 8291 Abschnitt 5 — der erzeugte Kopf stimmt Byte fuer Byte mit dem Beispiel der Norm, UND der private Schluessel der Norm macht unseren Chiffretext auf und liefert ihren Klartext. (5) P2: Die DSGVO-Loeschung erreicht die Zustellwege nicht. notification_log und push_subscriptions haengen per ON DELETE CASCADE an profiles — nur wird profiles in keinem Loeschpfad geloescht, sondern anonymisiert, die Kaskade feuert also nie. Nach einer vollstaendigen Loeschung nach Art. 17 standen weiter da: das Postfach mit Datum, Betrag und Bestellnummer, der Geraete-Endpunkt als aktives Zustellziel, und die E-Mail-Adresse in wait_list, die von keinem Loeschpfad je angefasst wurde (die Warteliste kennt kein Konto und hat nirgends eine Abmeldung). Jetzt: account/delete loescht die Push-Abos sofort und traegt die Adresse aus der Warteliste aus, solange sie bekannt ist (dasselbe Update leert profiles.email); der hard-delete-Cron raeumt notification_log und push_subscriptions. (6) P2, per Musterabgleich gefunden: nach dem ersten 42P10-Befund wurde jede der neun upsert-Stellen gegen ihren Index geprueft. Sechs sind in Ordnung. POST /api/favorites war es nicht: der Index aus 20260827_favorites_equipment.sql ist PARTIELL (WHERE equipment_id IS NOT NULL), und ein partieller Index kommt als Arbiter nur mit mitgeliefertem Praedikat in Frage — PostgREST schickt ueber on_conflict= nur die Spaltenliste, also nie. favorites.equipment_id ist live vorhanden, die Migration mithin eingespielt: jeder Klick auf 'Inserat merken' lief in 42P10 und kam als 500 zurueck. Die Salon-Seite derselben Route war nicht betroffen (voller Constraint). Jetzt reines INSERT — braucht keinen Arbiter, und 'schon gemerkt' galt dort seit jeher als Erfolg. (7) P3: /api/cookies/consent gab die rohe DB-Meldung zurueck (letzter Nachzuegler aus Track 18/19, auf einer unangemeldeten Route), hatte kein eigenes Limit, und liess ip_hash leer — eine Spalte, die live genau dafuer existiert. Ein Einwilligungsnachweis ohne Zuordnung ist keiner; die session_id kommt aus dem Browser. Jetzt dbError(), 20/h je IP, HMAC statt IP. Nachtrag: die Kachel 'Benachrichtigungen' im Konto trug fest verdrahtet 'Aktiv' — aus keiner Quelle gelesen, und einen Schalter dazu gibt es nirgends. Ohne Befund: /api/notifications (beide Zweige filtern auf user_id aus der Session), Empfaengerwahl aller acht createNotification-Aufrufe samt CHECK-Positivliste der Typen, anon-Rechte auf allen vier Tabellen (42501), Admin-Pruefung von push/send, analytics/visit. Nicht abschliessend pruefbar: customer_salon_history hat im Repo kein CREATE TABLE. WERKZEUG — der eigentliche Grund, warum nichts davon aufgefallen ist: src/test/fake-supabase.ts kannte gar kein upsert() (wer einen Test schreiben wollte, bekam 'upsert is not a function' und liess es), und die zweite Fassung im e2e-Harness nahm die Konfliktspalten einfach als Schluessel, ohne nach einem Index zu fragen. Beide nachgezogen: 42P10 statt stillem Rueckfall, Ausdrucks-Indizes bewusst gar nicht darstellbar, dazu addUniqueIndex()/requireArbiterIndex() (opt-in) und echte 23505-Durchsetzung beim INSERT inklusive NULL-Semantik und partieller Indizes. schema-probe.sh und live-schema.ts um push_subscriptions, wait_list und cookie_consents erweitert, favorites.equipment_id korrigiert. Zwei Bestandstests angepasst (Track-18-Mock von saveSubscription lieferte void, die Route liest jetzt ein Ergebnis; ip-hash-Zeitlimit auf 30s wegen Flake im vollen Lauf). Gegenprobe: mit auf bf6fcf6 zurueckgesetztem Produktivcode fallen 21 der 48 neuen Tests durch. 48 neue Tests (1526 -> 1574), Bericht und PDF in docs/reports/. ANMERKUNG: eine parallele Session im selben Arbeitsverzeichnis hat mit git add -A neun Dateien dieses Tracks in Commit 5af4013 eingesammelt, der von etwas anderem handelt — inhaltlich ist nichts verloren. Aus derselben Quelle: die CM22-Migration ist live angewendet (nachgeprueft: rental_equipment antwortet anon jetzt 401).
5af4013 CM22 Migration PROVEN_LIVE: Ledger aktualisiert
bf6fcf6 Track 22: Miet-Marktplatz — sieben Befunde, sechs behoben, einer als Migration. (1) Die 14-Tage-Freischaltung der Miet-Bewertungen ist NIE gelaufen: publish_review_pair() loest das Buchungsende ausschliesslich aus 'bookings' auf, Miet-Bewertungen tragen dort seit Migration 20260702 ABSICHTLICH eine rental_bookings.id (der Fremdschluessel wurde genau dafuer entfernt). Der SELECT findet keine Zeile, v_booking_ended_at bleibt NULL, 'IF NULL >= 14' ist nicht wahr — Fall 2 wurde nie betreten. Fall 1 (beide Seiten haben bewertet) braucht das Buchungsende nicht und lief weiter; deshalb ist es nie aufgefallen. Die Funktion gibt VOID zurueck und meldet keinen Fehler, wenn sie nichts findet: rpc() lieferte error null, der naechtliche Cron zaehlte published++ und meldete ok. Track 20 hat genau hier das Fehler-Handling repariert — die Meldung blieb trotzdem falsch, weil der Aufruf wirklich gelingt, er tut nur nichts. Die Oberflaeche verspricht woertlich 'spaetestens nach 14 Tagen'. Folge: jede einseitige Bewertung — der Normalfall — lag unbegrenzt als Entwurf, und avg_rating_as_tenant/as_provider blieben leer, weil der Trigger nur published zaehlt. Der Cron schaltet jetzt selbst frei (created_at ist eine konservative Naeherung, weil erst nach Buchungsende bewertet werden kann); die Funktion ist zusaetzlich in der Migration polymorph repariert. (2) Der Miet-Zweig des Stripe-Webhooks war der EINZIGE ohne Compare-and-Swap: Termin und Bestellung setzen seit Track 16 .neq('payment_status','paid').select(), die Miete nur .eq('id'). Der Lesecheck darueber ist eine Momentaufnahme, und was hinter dem Update kommt (payments, platform_transactions, Provision, Audit, zwei Benachrichtigungen) braucht genug Zeit fuer Stripes Timeout und dessen Wiederholung. Zwei Zustellungen ergaben zwei payments-Zeilen fuer EINE Miete — und payments ist die Quelle jeder Umsatzzahl in mis, kpi und investor. (3) rental_equipment ist mit dem oeffentlichen ANON-Key vollstaendig lesbar (live gesondet: 200, 5 Zeilen; rental_bookings, rental_requests, payout_accounts dagegen 42501). Die Policy USING(true) aus 20260819 begruendet sich mit 'der Browser-Client liest rental_equipment' — das stimmt seit Track 7 nicht mehr, GENAU WEIL salons fuer anon an is_admin_or_super scheitert und die Suche deshalb auf /api/rental-listings umgestellt wurde. Offen lagen damit unveroeffentlichte Entwuerfe (ensurePrimaryListing legt fuer jeden Vermieter eine is_available=false-Zeile an, jeder Preis davor steht ab dem Speichern oeffentlich) und die Inserate gesperrter Anbieter — der Riegel aus Track 15/20 war ueber PostgREST in einem Request zu umgehen. Eine engere Policy geht nicht: die Bedingung steht in salons, und eine Unterabfrage dorthin laeuft fuer anon in dasselbe is_admin_or_super. Deshalb REVOKE. NICHT BEHOBEN — committete Migration, kein Runner, kein DB-Zugang. (4) Unbezahlte Reservierungen zaehlten als Umsatz: countsAsRevenue hing allein am status, 'pending' steht auf keiner Ausschlussliste. Die Umsatzseite filtert genau auf dieses Feld und macht daraus Einnahmen, Auslastung und eine Preisempfehlung — waehrend ihr eigener Leerzustand verspricht 'sobald die erste Buchung BEZAHLT ist'. Solche Zeilen kosten nichts: die Buchung entsteht vor dem Checkout und bleibt bis zum naechtlichen Cleanup (bis ~28h). Jetzt Positivliste auf dem Zahlungsstatus. (5) /api/rental-requests ohne eigenes Limit: der Dedupe-Claim verhindert denselben INHALT, ein Zeichen mehr ergibt eine neue Mail; die Mail-Idempotenz haengt an der Anfrage-ID, und jede neue Anfrage hat eine neue. Uebrig blieb das Middleware-Limit — bis zu 86.400 Mails pro Tag an ein Vermieter-Postfach mit unserer Absenderreputation. Jetzt 20/h je Konto plus 10/h je Mietobjekt, beide vor dem DB-Lookup. (6) UTC statt Berlin in /api/rental-requests (Anfrage fuer gestern zwischen 0 und 2 Uhr — Track 18 hat genau das in der Buchung behoben) und im Payout-Cron (die Auszahlung haengt an der Cron-Startzeit statt an der Miete). (7) Datumsangaben nur auf ihre Form geprueft: 2026-13-45 wird NaN, und NaN ist an 'days > 366' UND an 'totalCents <= 0' beides falsch — beide Riegel liessen es bis in den Insert durch, total_cents NaN serialisiert als null. 2026-02-30 rollt in JS still auf den 2. Maerz, der Preis stand also fuer einen anderen Zeitraum fest als den, den Postgres dann mit 22008 abweist. Neuer Baustein src/lib/iso-date.ts. Migration ausserdem mit sieben CHECK-Constraints, die Regeln festschreiben, die heute nur im Route-Handler stehen (Enddatum, Betrag, Zahlungsstatus, Objekttyp — der bestimmt den Provisionssatz —, Preise, 'online nur mit Tagespreis' steht dreimal im Code und nirgends in der DB, Verfuegbarkeitsfenster); jeder zaehlt vorher die verletzenden Zeilen und bricht mit klarer Meldung ab statt still zu aendern. Ohne Befund: serverseitige Preisquelle, Provision, Escrow und Auszahlungspruefungen, Mandantentrennung der Vermieter-Routen, Storno-Berechtigung, Statuswechsel der Anfrage, Bewertungs-Berechtigung, Anbieter-Sperre, IBAN-Handhabung, anon-Sperre aller uebrigen Miet-Tabellen, Zombie-Buchungs-Cleanup. Harness um raceBefore() erweitert — ohne eine Stelle fuer das Fenster zwischen SELECT und UPDATE ist ein CAS nicht von einem gewoehnlichen Update zu unterscheiden. Zwei Track-20-Tests angepasst (der RPC-Aufruf existiert nicht mehr; der Erfolgsfall prueft jetzt strenger, dass die Zeile wirklich published ist), zwei Bestandsdateien mit __resetRateLimits(), Zeitlimit eines Track-21-Tests auf 30s (Flake im vollen Lauf). Gegenprobe: mit zurueckgesetztem Produktivcode fallen 10 der 20 neuen und angepassten Tests durch. 18 neue Tests (1508 -> 1526), Bericht und PDF in docs/reports/
2737dde CM Track 21: Auth, Sessions und Mandantentrennung — sieben Befunde, alle behoben. (1) Zwei-Faktor-Authentifizierung liess sich mit EINEM POST abschalten: /api/auth/2fa/setup schrieb bedingungslos 'enabled: false' per upsert. Auf einer Zeile mit enabled=true ist das keine Einrichtung, sondern eine Abschaltung — verlangt wurde dafuer nichts: kein aktueller TOTP-Code, kein Passwort, allein das Sitzungs-Cookie. Der Login prueft genau diese Spalte (Track 17), fragte danach also wieder nur nach dem Passwort — bei einem Konto mit 2FA gerade der Teil, den man als kompromittiert annehmen muss. Eine Route zum absichtlichen Deaktivieren gibt es gar nicht; das war eine versehentliche. Jetzt 409 und kein Schreibvorgang, fail closed bei Lesefehler. (2) Ein Passwortwechsel beendete KEINE offene Sitzung. Das Cookie laeuft 365 Tage und hing am Passwort nicht: 'dieses Passwort gilt nicht mehr' stand weder im Token noch in der Datenbank. Wer sein Passwort aendert, WEIL jemand anderes in seinem Konto ist, hat diesen Jemand damit nicht ausgesperrt. Beim Reset kam eine zweite Ursache dazu — /auth/reset-password laeuft vollstaendig im Browser gegen Supabase-Auth, der Server erfaehrt davon gar nichts. Jetzt: token.loginAt beim Login (nicht iat — der Rolling-Refresh setzt iat alle 24h neu, eine gestohlene Sitzung haette sich selbst an jedem Widerruf vorbeigeschoben), Widerruf als SESSION_REVOKED in audit_logs (profiles hat live keine passende Spalte, und eine Auswahl auf eine fehlende Spalte macht mit 42703 die GANZE Abfrage kaputt), Pruefung in getServerSession, neue Route POST /api/auth/session-revoke als serverseitiges Ende des Resets — Ausweis ist das Supabase-Zugangstoken, geprueft ueber auth.getUser, die Nutzer-ID kommt aus der Antwort und nie aus dem Request. (3) Das Sitzungs-Cookie hiess fest 'authjs.session-token' und hatte damit keinen __Secure-Praefix. Cookie-Setzen kennt keine Herkunftstrennung: jede Subdomain, auch eine ueber HTTP, kann ein gleichnamiges Cookie mit Domain=.chairmatch.de setzen und das echte ueberschreiben — Session-Fixation. Das secure-Flag hilft nicht, es regelt das Senden, nicht das Setzen. (4) Ein admin konnte jeden super_admin herabstufen: geprueft wurde nur die NEUE Rolle, nie die bestehende des Ziels. Selbst hochstufen ging nicht — aber die einzige Rolle abraeumen, die ihn haette zurueckstufen koennen. Dazu fehlte der Schutz gegen die eigene Aussperrung. (5) bookings.staff_id kam ungeprueft aus dem Request. staff ist mandantengetrennt; eine fremde ID schreibt den Termin eines Betriebs auf eine Person eines anderen. getAvailableSlots filtert bereits auf staff_id — ab dem Tag, an dem die Planung pro Person eingeschaltet wird, waere das ein Hebel in den Kalender eines fremden Salons. (6) /api/recommendations las bookings.user_id — diese Spalte gibt es live nicht (sie heisst customer_id). Jeder Aufruf, auch der richtige, bekam 42703 als 'Buchung nicht gefunden'; die Autorisierungspruefung darunter ist nie gelaufen, Track 17 war an dieser Route ungetestete Theorie. Dazu ungeprueftes staffId (fremder Mitarbeiter mit Name und Funktion in der eigenen Kundin ihrem Feed) und productId. (7) Der Super-Admin-Upload nahm den Ordner roh aus dem FormData in den Storage-Pfad. Nachtrag: die 2FA-Kachel im Konto zeigte nach dem Klick 'Aktiv', obwohl die Route enabled=false geschrieben hatte — und einen Ort zum Bestaetigen des Codes gab es nirgends; jetzt mit zweitem Schritt. Ohne Befund: Mandantentrennung der Anbieter-Routen, Postfach, Compliance, Uploads, Stripe-Webhook-Signatur, Registrierung, Passwort-Reset-Orakel, Middleware-Default-Deny ueber alle 106 Routen; einen Einladungsflow gibt es nicht. Offen und ausdruecklich NICHT als behoben ausgegeben: die live angewendeten RLS-Policies sind ohne DB-Zugang nicht pruefbar. OPERATIVE FOLGEN: alle Sitzungen enden einmalig (Cookie-Name), ein Passwortwechsel meldet ab sofort alle Geraete ab, 2FA laesst sich nicht mehr ueber die Kachel zuruecksetzen. Harness um auth.getUser erweitert. Gegenprobe: mit zurueckgesetztem Produktivcode fallen 29 der 51 neuen Tests durch. 51 neue Tests (1457 -> 1508), Bericht und PDF in docs/reports/
0d00473 Track 20 Nachtrag: drei Typfehler in der neuen Testdatei. phoneSend ist mit withApi() gewickelt und hat damit die Signatur (req, ctx) — die drei Aufrufe uebergaben nur den Request. Der Guard in deploy.sh prueft typecheck nur warn-only, der erste Commit ging deshalb mit den Fehlern durch; vitest hat sie nicht gesehen, weil zur Laufzeit ein fehlendes ctx schlicht undefined ist. Jetzt uebergeben alle drei ausdruecklich undefined. Keine Verhaltensaenderung, 27 Tests weiterhin gruen, tsc --noEmit sauber.
```

---

## Vercel / Production

| Feld | Wert |
|------|------|
| Production URL | https://chairmatch.de |
| Project ID | prj_SeoqYHyYwv4tZV8skY8Ag5Dw8iuS |
| Dashboard | https://vercel.com/team_iJXOJqpBTNdePfg1tMV0r1ip/chairmatch/deployments |

### Latest Deployment

```
(kein VERCEL_TOKEN gesetzt — siehe Dashboard)
```

---

_Refresh: `./scripts/status.sh` — läuft automatisch am Ende von `./deploy.sh`._
